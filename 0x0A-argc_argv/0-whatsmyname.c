#include "main.h"

/**
 * main - the function that prints the user name
 * @argc: The count of arguments
 * @argv: a pointer to an array of argument(as pointer)
 *
 * Return: 0 for success
 */

int main(int __attribute__ ((unused)) argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
