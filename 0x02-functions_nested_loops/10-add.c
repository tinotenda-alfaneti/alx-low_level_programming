#include "main.h"
/**
 * add - takes two integers and add them together
 * @x: first number
 * @y: the second number
 *
 * Return: sum of the two number
 */
int add(int x, int y)
{
	return (x + y);
}
