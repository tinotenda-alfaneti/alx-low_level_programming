#include "main.h"

/**
 * _strlen - length of a string
 * @s: string to count
 *
 * Return: string length
 */
int _strlen(char *s)
{
	int i = 0;

	for (; *s != '\0'; s++)
	{
		i++;
	}

	return (c);
}
